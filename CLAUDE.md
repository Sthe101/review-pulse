# ReviewPulse
AI-powered customer review analysis SaaS.

## Tech Stack
Next.js 14, TypeScript, Tailwind, Supabase, Anthropic Claude API, Stripe, Vercel

## Architecture
- Route groups: (marketing), (auth), (dashboard)
- Design reference: design-reference.jsx
- PDF/CSV templates: reviewpulse-export-templates.jsx
- Validators: skills/ directory

## Build Progress

### Step 1 — Project scaffold & design system
- Next.js 14 app scaffolded with TypeScript, Tailwind, App Router, `src/` dir (`npx create-next-app@latest`).
- `tsconfig.json`: `strict: true`, `noUncheckedIndexedAccess: true`.
- Dependencies installed: `@supabase/supabase-js`, `@supabase/ssr`, `@anthropic-ai/sdk`, `stripe`, `@stripe/stripe-js`, `zustand`, `zod`, `papaparse`, `sonner` (+ `@types/papaparse`).
- `.env.local.example` — all 13 env var names (Supabase, Anthropic, Stripe, Google Business, encryption, app URL).
- `.gitignore` — `node_modules`, `.next`, `.env.local`, `.env`, `.vercel`, `*.tsbuildinfo`.
- `tailwind.config.ts` — extends brand colors: teal `#0D9488`, navy `#1E3A5F`, coral `#EA580C`, pos `#059669`, neg `#DC2626`, warn `#D97706`. `darkMode: "class"`.
- `src/app/globals.css` — CSS custom properties for light (`:root`) and dark (`.dark`), with `--tx3` darkened to `#64748B` (light) / `#7A8BA8` (dark) for WCAG AA contrast. Utility classes: `.card`, `.card-click`, `.btn` + 5 variants (`coral`, `navy`, `teal`, `outline`, `danger`), `.badge`, `.sidebar` + `.collapsed`, `.mobile-bar`, `.overlay`. `@keyframes spin` + `pulse`, `prefers-reduced-motion`, `*:focus-visible` 2px teal outline, 768px / 480px media queries.
- `src/stores/theme-store.ts` — Zustand with `persist` middleware (localStorage `reviewpulse-theme`); `toggle()` flips state + toggles `.dark` on `documentElement`, `hydrate()` for mount sync.
- `src/components/theme-provider.tsx` — client provider, runs `hydrate()` on mount.
- `src/components/ui/` — `icon.tsx` (typed `IconName` union, 35 icons incl. `sun`/`moon`/`bell`; `$` renamed to `dollar`), `button.tsx` (5 variants × 3 sizes + `fullWidth` + `loading` Spinner), `card.tsx` (clickable with keyboard support), `badge.tsx` (5 variants + custom color/bg), `modal.tsx` (portal, backdrop blur, Esc, click-outside, focus trap, `role="dialog"`/`aria-modal`), `spinner.tsx` (sm/md/lg, `role="status"`), `skeleton.tsx`, `empty-state.tsx`, `sentiment-bar.tsx` (percentage aria-label), `donut-chart.tsx` (center score + aria-label), `toast-provider.tsx` (sonner, bottom-right).
- `src/components/layout/` — `dashboard-shell.tsx` (sidebar + collapse + mobile drawer + overlay + usage gauge + user menu + theme toggle), `landing-layout.tsx` (sticky nav + footer, `next/link`).
- `src/app/layout.tsx` — `html lang="en"`, metadata template `"%s — ReviewPulse"` / default `"ReviewPulse"`, wraps `ThemeProvider` + `ToastProvider`.
- `src/app/page.tsx` — launch hero with inline SVG logo + "ReviewPulse" + tagline.
- `src/app/test/page.tsx` — renders every UI component in every variant (temporary; deleted in Feature 4.1).
- `.claude/hooks/evaluate.sh` — rewrote to use Python instead of `jq` (jq not installed on this Windows/git-bash machine; every PreToolUse hook was erroring with exit 127 due to `set -euo pipefail`).
- Logo fix: lowered navy bar (viewBox `0 0 32 32`) from `y=10 h=18` to `y=12 h=16` in both `page.tsx` and `icon.tsx` — the orange curve's stroke (width 2.5) now fully covers the bar top instead of the bar peeking through.
- Build: `npm run build` → 0 errors, 3 static routes (`/`, `/_not-found`, `/test`).

### Step 2 — Supabase schema & client setup
- `supabase/migrations/001_initial_schema.sql` — 8 tables: `profiles` (extends `auth.users`, plan enum `free`/`pro`/`business`, Stripe ids, monthly usage counter, onboarding checklist JSONB), `projects` (industry enum, `is_demo` flag), `reviews` (content 1–10k chars, rating 1–5, sentiment enum, `themes TEXT[]`), `analyses` (sentiment breakdown 0–100, `overall_score`, complaints/praises/feature_requests/action_items JSONB), `notification_prefs`, `integrations` (9 platforms incl. `google_business`/`yelp`/`trustpilot`/`app_store`/`play_store`/`amazon`/`g2`/`slack`/`zapier`, encrypted tokens, unique `(user_id, platform, platform_account_id)`), `sync_logs`, `shared_reports` (16-byte hex `share_token` via `gen_random_bytes`, password hash, expiry, view counter).
- RLS enabled on all 8 tables. Policies: owner-only `FOR ALL` on most; `profiles` split into SELECT/UPDATE/INSERT; `reviews`/`analyses`/`sync_logs` scope via parent ownership subquery; `shared_reports` has extra public SELECT policy when `is_active = true`.
- Triggers & functions: `public.handle_new_user()` (SECURITY DEFINER) auto-inserts `profiles` + `notification_prefs` rows on `auth.users` insert; `reset_monthly_usage()` zeros `reviews_used_this_month` and bumps `billing_cycle_start` for rows older than 30 days (wire to cron later).
- Indexes: `projects(user_id)`, `reviews(project_id)`, `reviews(sentiment)`, `analyses(project_id)`, `analyses(created_at)`, `integrations(user_id)`, partial `integrations(status) WHERE status='connected'`, `sync_logs(integration_id)`, `shared_reports(share_token)`.
- `src/lib/supabase/client.ts` — `createBrowserClient<Database>` from `@supabase/ssr`, reads `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`.
- `src/lib/supabase/server.ts` — `createServerClient<Database>` with `await cookies()` (Next 15 async API) using the `getAll`/`setAll` cookie interface; try/catch around `setAll` for Server Component safety.
- `src/lib/supabase/middleware.ts` — `updateSession(request)` helper: refreshes auth token via `supabase.auth.getUser()`, propagates cookies through a rewritten `NextResponse`. Returns `{ supabaseResponse, user }` so the top-level middleware can make routing decisions without a second Supabase round-trip. (Wired in Step 4.)
- `src/types/database.ts` — row interfaces (`Profile`, `Project`, `Review`, `Analysis`, `NotificationPrefs`, `Integration`, `SyncLog`, `SharedReport`) + enum unions (`Plan`, `Industry`, `Sentiment`, `IntegrationPlatform`, `IntegrationStatus`, `SyncStatus`) + `OnboardingChecklist`, `AnalysisItem`, `ActionItem`. Exports `Database` generic (`public.Tables.*` with `Row`/`Insert`/`Update`) consumed by all three clients.
- `pgcrypto` extension required for `gen_random_uuid()` / `gen_random_bytes()` — enabled by default on Supabase.

### Step 3 — Testing stack (Vitest + Playwright)
- Dev deps: `vitest@4`, `@vitejs/plugin-react`, `@testing-library/react`, `@testing-library/dom` (peer of `/react`, had to be installed explicitly — first `npm test` run failed with `Cannot find module '@testing-library/dom'`), `@testing-library/jest-dom`, `jsdom`, `@vitest/coverage-v8`, `@playwright/test`. Chromium browser installed via `npx playwright install chromium` (~290 MB incl. headless shell).
- `vitest.config.ts` — `plugins: [react()]`, `environment: 'jsdom'` default, `environmentMatchGlobs` routes `src/__tests__/integration/**/api/**`, `**/*.api.test.ts`, and `src/app/api/**/*.test.ts` to node. `setupFiles: ['src/test/setup.ts']`, `globals: true`, `@/*` alias via `path.resolve(__dirname, './src')`, v8 coverage with `text` + `html` reporters, excludes node_modules/.next/e2e/test/*.config.
- `playwright.config.ts` — `testDir: './e2e'`, `baseURL` from `PLAYWRIGHT_TEST_BASE_URL` or `http://localhost:3000`, `webServer` runs `npm run dev` with `reuseExistingServer: !process.env.CI` and 120s boot timeout, `screenshot: 'only-on-failure'`, `trace: 'retain-on-failure'`, single `chromium` project, `retries: 1` on CI else 0.
- `src/test/setup.ts` — imports `@testing-library/jest-dom/vitest` + runs `cleanup()` in `afterEach`.
- `src/test/mocks/supabase.ts` — `createMockSupabaseClient()` factory. Internal `createQueryBuilder()` returns a chainable object where 20 builder methods (`select`/`insert`/`update`/`upsert`/`delete`/`eq`/`neq`/`gt`/`gte`/`lt`/`lte`/`like`/`ilike`/`in`/`is`/`or`/`contains`/`order`/`limit`/`range`/`match`) each `vi.fn(() => builder)`. Terminal: `single`/`maybeSingle` resolve to `{ data, error: null }`; `.then` makes the builder itself awaitable. `auth` surface covers `getUser`/`getSession`/`signInWithPassword`/`signUp`/`signOut`/`exchangeCodeForSession`/`resetPasswordForEmail`/`updateUser`/`onAuthStateChange`. Also `storage.from()` (upload/download/remove/getPublicUrl) and `rpc`.
- `src/test/mocks/stripe.ts` — `createMockStripe()` with `customers` (create/retrieve/update/del), `subscriptions` (create/retrieve/update/cancel/list), `invoices` (retrieve/list/retrieveUpcoming), `webhooks.constructEvent` (parses payload into a fake `customer.subscription.created` event), `billingPortal.sessions.create`, `checkout.sessions.create/retrieve`.
- `src/test/mocks/anthropic.ts` — `createMockAnthropic(response?)` with `messages.create` returning a realistic `{id, role, model: 'claude-opus-4-7', usage, content: [{type:'text', text: JSON.stringify(response)}]}`; defaults to `sampleAnalysisResponse` from fixtures. `messages.stream` stubbed as `vi.fn()`.
- `src/test/fixtures/reviews.ts` — `positiveReviews` (3), `negativeReviews` (3), `mixedReviews` (positives + negatives + 2 neutral/3-star), `emptyReviews`, `largeBatchReviews` (250 generated, rating cycles 1–5, source cycles Google/Yelp/Trustpilot/G2).
- `src/test/fixtures/analysis.ts` — `sampleAnalysisResponse` matching the planned Zod schema: `summary`, `sentiment` {positive/neutral/negative/mixed} summing ~100, `overall_score`, `complaints` (with severity + examples), `praises`, `feature_requests`, `action_items` (title/description/priority), `rating_distribution` {"1"-"5"}.
- `e2e/helpers/auth.ts` — `loginAsTestUser(page, email, password)` fills `/login` form and waits for `/dashboard`. `signupTestUser(page)` generates `test+<ts>@reviewpulse.test` + `TestPass!<ts>`, submits `/signup`, waits for `/dashboard|/onboarding`, returns `{email, password}`.
- `e2e/helpers/seed.ts` — `seedTestData({userId, projectName?})` uses service-role admin client (throws if `NEXT_PUBLIC_SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` missing). Inserts one project (`is_demo: true`, industry `SaaS`) + all `mixedReviews` + one analysis row populated from `sampleAnalysisResponse`. Returns `{projectId, reviewIds, analysisId}`.
- `src/__tests__/unit/sanity.test.ts` — trivial `expect(1+1).toBe(2)`. `e2e/sanity.spec.ts` — `page.goto('/')` + `expect(page.locator('text=ReviewPulse')).toBeVisible()`. `src/__tests__/integration/.gitkeep` stubs the dir.
- `package.json` scripts: `test` (`vitest run`), `test:watch` (`vitest`), `test:e2e` (`playwright test`), `test:coverage` (`vitest run --coverage`), `test:all` (sequential vitest → playwright).
- Verified: `npm test` → 1 passed in ~10s. `npx playwright test` → 1 passed (2.8s test + ~28s Next dev boot).

### Step 4 — Auth middleware & routing
- `src/middleware.ts` — top-level Next middleware. Route logic:
  - **Bypass prefixes** (`/api/stripe/webhook`, `/api/cron`, `/report`) → `NextResponse.next()` before any Supabase call (webhooks/cron must never be redirected or incur an `auth.getUser()` round-trip).
  - Otherwise calls `updateSession(request)` which refreshes the session cookie and returns the user.
  - **Protected prefixes** (`/dashboard`, `/projects`, `/trends`, `/integrations`, `/settings`, `/billing`; matched on exact + `/` subpaths): unauthenticated → 302 `/login?next=<pathname>`.
  - **Auth pages** (`/login`, `/signup` exact): authenticated → 302 `/dashboard`.
  - On every redirect, refreshed cookies from `supabaseResponse` are copied onto the redirect response via `supabaseResponse.cookies.getAll().forEach(c => redirect.cookies.set(c))` so the session isn't lost across the 302.
  - `config.matcher` excludes `_next/static`, `_next/image`, `favicon.ico`, and `svg`/`png`/`jpg`/`jpeg`/`gif`/`webp`/`ico` asset requests.
- `src/lib/supabase/middleware.ts` — updated: `updateSession` now returns `{ supabaseResponse, user }` (previously just the response). Required so `src/middleware.ts` can branch on auth state without a second Supabase call.
- Build-fix fallout from wiring this up on Vercel:
  - `src/test/mocks/supabase.ts` — `builder` widened from `Record<string, ReturnType<typeof vi.fn>>` to `Record<string, unknown>`; the `.then` assignment (plain function, not a `vi.fn`) was failing `next build`'s TS pass.
  - `vitest.config.ts` — `environmentMatchGlobs` removed (deleted in Vitest 4, was breaking `next build`'s type-check of the config file). Replacement: API/Node-only tests add the per-file pragma `// @vitest-environment node` at the top.
- Next 16 deprecation warning (non-blocking): `⚠ The "middleware" file convention is deprecated. Please use "proxy" instead.` Build log now labels it `ƒ Proxy (Middleware)`. Left as `middleware.ts` for now; migration is rename file → `proxy.ts` + export `proxy` instead of `middleware`.
- Verified: `npm run build` → compiled + TS passed + 3 static routes (`/`, `/_not-found`, `/test`) + `ƒ Proxy (Middleware)`. `npm test` → 1 passed.

### Step 5 — Middleware unit tests
- `src/__tests__/unit/middleware.test.ts` — 5 tests, all passing (~2.3s).
  - File-level pragma `// @vitest-environment node` — `next/server`'s `NextRequest`/`NextResponse` need a Node runtime, not jsdom. This is the pattern called out in `vitest.config.ts` after `environmentMatchGlobs` was removed in Vitest 4.
  - `vi.mock('@/lib/supabase/middleware', () => ({ updateSession: vi.fn() }))` — hoisted mock so the real Supabase client never gets constructed. Each test sets the return with `mockUpdateSession.mockResolvedValue({ supabaseResponse: NextResponse.next(), user })`.
  - Request factory: `new NextRequest(new URL('http://localhost:3000' + pathname))`.
  - Redirect assertions are tolerant on status (`[301, 302, 307, 308]`) — Next's `NextResponse.redirect` defaults to 307 but don't want the test to break if that default changes. Location assertions parse the `Location` header as a URL and check `pathname` + `searchParams.get('next')` precisely.
  - Bypass tests (`/api/stripe/webhook`, `/report/abc`) assert **both** that no redirect was issued **and** that `updateSession` was never called — proves the short-circuit happens before any Supabase work, not just that we didn't redirect.
  - Landing test (`/`) runs twice with `mockResolvedValueOnce(null)` then `mockResolvedValueOnce(fakeUser)` and confirms no redirect in either state.
  - `beforeEach(() => mockUpdateSession.mockReset())` to isolate call counts per test.

### Step 6 — Signup page & tests
- `src/app/(auth)/layout.tsx` — centered route-group layout. Logo is a `next/link` back to `/` with `aria-label="ReviewPulse home"` (used by E2E locator). Content wrapped in `max-width: 420px` container.
- `src/app/(auth)/signup/page.tsx` — `"use client"`. Fields: full name, email, password, each rendered by a local `Field` component that absolutely-positions an `<Icon>` inside the input (icons: `usr`/`mail`/`lock`) and paints the border red via `border-color: var(--neg)` when the field has an error. `EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/`. `passwordStrength()` scores 0–4 (0 empty, 1 <8, 2 ≥8, +1 if mixed case, +1 if digit + special). `StrengthBar` renders 4 segments with `role="progressbar"` + `aria-valuemin/max/now` (queried by the unit test). Colors: 4=`--pos`, 3=`#F59E0B`, 2=`--warn`, 1=`--neg`. Submit calls `supabase.auth.signUp({ email, password, options: { data: { full_name: name.trim() } } })`. Existing-account detection runs three checks: `error.code === 'user_already_exists'`, `error.message` contains `already registered`/`already exists`, OR success-shaped response with `data.user.identities` empty (Supabase returns a fake user on duplicate when email confirmations are on, to prevent enumeration). On success → `router.push('/onboarding')`. Other errors go through `toast.error()`. `<Button variant="coral" fullWidth loading={loading}>` is the existing design-system button; loading state swaps label to `"Creating account…"` and disables via the built-in `loading` prop.
- `src/__tests__/unit/signup.test.tsx` — 6 tests, all passing (~700ms). Uses `vi.hoisted(() => vi.fn())` for `pushMock`, `signUpMock`, `toastErrorMock` so they're available when `vi.mock()` factories run. Mocks `next/navigation` → `{ useRouter: () => ({ push: pushMock }) }`, `@/lib/supabase/client` → `{ createClient: () => ({ auth: { signUp: signUpMock } }) }`, `sonner` → `{ toast: { error: toastErrorMock } }`. Strength-bar test reads `aria-valuenow` off `role="progressbar"` (1 for `"abc"`, 4 for `"Abcdefg1!"`). Success test asserts `signUp` called with `{ email, password, options: { data: { full_name: "Jane Doe" } } }`.
- `e2e/signup.spec.ts` — 5 tests, all passing (~20s). `page.route('**/auth/v1/signup**', mockSignupSuccess)` fulfills with a flat Supabase User payload (no wrapping object — GoTrueClient treats body without `access_token` as a User) including non-empty `identities: [{id: 'i_e2e_1', ...}]` so the "existing account" branch doesn't fire. Locators use role/label queries (`getByLabel(/full name/i)`, `getByRole('link', { name: /reviewpulse home/i })`). The logo test confirms the `(auth)` layout wires the logo Link correctly. The `/login` test passes even though the route doesn't exist yet — Playwright's `waitForURL` matches on URL regardless of HTTP status, so a 404 at `/login` still satisfies it.
- `playwright.config.ts` — added `webServer.env` fallbacks (`NEXT_PUBLIC_SUPABASE_URL` → `https://test.supabase.co`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` → `test-anon-key-placeholder`) so the dev server Playwright spawns boots cleanly in CI / clean checkouts. Real env via `process.env.*` preserved through `??` fallback. The fallback URL is valid-looking enough for `createBrowserClient()` constructor to succeed; actual HTTP calls are intercepted via `page.route()`.
- Verified: `npm test` → 6 passed in ~8.3s. `npx playwright test e2e/signup.spec.ts` → 5 passed in 20.5s.

### Step 7 — Login page & tests
- `src/app/(auth)/login/page.tsx` — `"use client"`. Exports a `LoginPage` shell that wraps the real `LoginForm` in `<Suspense fallback={<Card>Loading…</Card>}>` — required because `LoginForm` calls `useSearchParams()`, and Next 16 Turbopack fails the static prerender of `/login` with `useSearchParams() should be wrapped in a suspense boundary` otherwise (confirmed by a first build attempt). Reuses the same local `Field` component pattern from signup (icon prefix, red border on error, `aria-invalid`/`aria-describedby`). Fields: email (icon `mail`, `autoComplete="email"`), password (icon `lock`, `autoComplete="current-password"`). Below them: a flex row with a "Remember me" checkbox (default `true`, `accentColor: var(--teal)`, renders but is not wired to session-persistence yet — `@supabase/ssr` cookies persist regardless) and a `<Link href="/forgot-password">` styled in teal. Submit calls `supabase.auth.signInWithPassword({ email, password })`. **Any** `error` from Supabase collapses to a single inline banner reading `"Invalid email or password"` (coral-tinted, `role="alert"`, `color-mix(in srgb, var(--neg) 10%, transparent)` background) — the raw Supabase message is never surfaced, so registered-vs-unregistered email isn't leaked. On success → `router.push(next && next.startsWith('/') ? next : '/dashboard')`, where `next` is read from `useSearchParams()`; the `startsWith('/')` check prevents open-redirect via absolute URLs smuggled into `?next=`. Unexpected throws go through `toast.error()` as a fallback. Bottom link: `"Sign up free"` → `/signup` (teal, weight 600).
- `src/__tests__/unit/login.test.tsx` — 4 tests, all passing (~700ms). `.tsx` extension because JSX. `vi.hoisted(() => vi.fn())` for `pushMock`, `signInMock`, `toastErrorMock`, `getSearchParamMock` (the last one defaulted to `() => null` so the success path lands on `/dashboard`, not on a `?next=…` target). Mocks `next/navigation` → both `useRouter: () => ({ push: pushMock })` and `useSearchParams: () => ({ get: getSearchParamMock })`; `@/lib/supabase/client` → `{ createClient: () => ({ auth: { signInWithPassword: signInMock } }) }`; `sonner` → `{ toast: { error: toastErrorMock } }`. The "generic error" test is the interesting one — it feeds Supabase's real `"Invalid login credentials"` string back through the mock and then asserts `queryByText(/invalid login credentials/i)` is **null** alongside `/wrong password/i`, `/no account/i`, `/user not found/i` — proves we normalize to the safe message rather than echoing whatever the server said. Also asserts `pushMock` not called on failure.
- Verified: `npm run build` → 4 static routes (`/`, `/login`, `/signup`, `/test`) + `ƒ Proxy (Middleware)`. `npx vitest run src/__tests__/unit/login.test.tsx` → 4 passed in ~8.9s.
- `e2e/login.spec.ts` — 5 tests, all passing (~19s). `mockLoginSuccess` intercepts `POST /auth/v1/token?grant_type=password` returning a full session payload (`access_token`, `refresh_token`, `token_type: "bearer"`, `expires_in`, user with non-empty `identities`) — GoTrueClient treats any body with `access_token` as a signed-in session. `mockLoginInvalid` returns `400` with `{code: "invalid_credentials", error: "invalid_grant", msg: "Invalid login credentials"}` shaped like the real Supabase error.
- Middleware/Playwright-route gotcha worked around: after mocked login success, `router.push('/dashboard')` triggers an RSC fetch for the protected route. Middleware runs server-side and calls `supabase.auth.getUser()`, which hits the real `NEXT_PUBLIC_SUPABASE_URL` (fallback `https://test.supabase.co`). **`page.route()` only intercepts browser-originated requests, not server-side fetches from middleware**, so that call fails and middleware bounces to `/login?next=%2Fdashboard`. The valid-credentials test accepts either `/dashboard` (if middleware happened to let it through) or `/login?next=%2Fdashboard` as proof that the client-side redirect fired — the middleware gating is already covered independently by `src/__tests__/unit/middleware.test.ts`. Assertion: `page.waitForURL(/\/dashboard(?:$|\?)|[?&]next=%2Fdashboard/, ...)` + explicit check that `pathname === '/dashboard'` or `searchParams.get('next') === '/dashboard'`.
- The "wrong password" test asserts **both** that `/invalid email or password/i` is visible AND `getByText(/invalid login credentials/i)` has count 0 — locks in that the raw Supabase wording never leaks into the UI. Also asserts the URL stays on `/login` (no mocked session → no redirect attempt in this path).
- Final verification: `npx playwright test e2e/login.spec.ts` → 5 passed in 19.2s.

### Step 8 — Forgot-password page & tests
- `src/app/(auth)/forgot-password/page.tsx` — `"use client"`. Single-state page with an `email`/`emailError`/`loading`/`sent` state quartet. Submit validates against the same `EMAIL_REGEX` used elsewhere, then calls `supabase.auth.resetPasswordForEmail(email, { redirectTo: \`${window.location.origin}/reset-password\` })` — `redirectTo` is where the magic link in the email will land (reset-password page is future work). On success flips `sent: true` and the render returns a totally different card: a 56px `color-mix(in srgb, var(--pos) 15%, transparent)` circle with a green `ok` checkmark icon, "Check your email" heading, a body copy "*If an account exists for **{email}**, we just sent a password reset link…*" (deliberately hedged so a registered-vs-unregistered email can't be sniffed out of the UI wording), and a coral `<Button>` inside a `<Link href="/login">` for "Back to login". Errors (including the rare Supabase error) go to `toast.error("Something went wrong. Please try again.")` — never echo Supabase's raw message. Below the form (pre-success view): a teal "← Back to login" link.
- Reuses the same local `Field` component as signup/login — no shared component extraction yet; each auth page has its own copy. (Will revisit if a fourth auth page materializes.)
- `src/__tests__/unit/forgot-password.test.tsx` — 3 tests, all passing (~570ms). `.tsx` extension. Mocks `@/lib/supabase/client` → `{ createClient: () => ({ auth: { resetPasswordForEmail: resetPasswordMock } }) }` and `sonner` → `{ toast: { error: toastErrorMock } }`. No `next/navigation` mock needed — the page uses `<Link>`, not `router.push`. Success-state test asserts `resetPasswordForEmail` called with `(email, { redirectTo: expect.any(String) })`, then waits for the "Check your email" heading, and double-confirms the form DOM has been swapped out by asserting the email input and submit button are no longer in the document. The "Back to login" test resolves through `<Link href="/login"><Button>Back to login</Button></Link>` via `getByRole("link", { name: /back to login/i })` (the anchor takes its accessible name from the inner button's text).
- `e2e/forgot-password.spec.ts` — 3 tests, all passing (~16.6s). `mockRecoverySuccess` intercepts `POST /auth/v1/recover` returning `{}` (Supabase's actual success body). Tests: form renders, submit → success state (asserts "Check your email" heading visible + email input/submit button have `.toHaveCount(0)`), "Back to login" link → `/login` (no middleware interference since `/login` isn't protected).
- Verified: `npm run build` → 5 static routes (`/`, `/forgot-password`, `/login`, `/signup`, `/test`) + `ƒ Proxy (Middleware)`. `npx vitest run src/__tests__/unit/forgot-password.test.tsx` → 3 passed. `npx playwright test e2e/forgot-password.spec.ts` → 3 passed.

### Step 9 — Google OAuth + auth callback route
- `src/components/auth/oauth-buttons.tsx` — single shared client component rendered at the bottom of both login and signup pages. "or continue with" divider (two 1-px `var(--bd)` lines + centered 12-px label in `var(--tx3)`, 500-weight), then a full-width `<Button variant="outline">` containing an inline multi-color Google "G" SVG (18 × 18, official palette — `#FFC107`, `#FF3D00`, `#4CAF50`, `#1976D2`) + "Continue with Google" text. Click handler calls `supabase.auth.signInWithOAuth({ provider: "google", options: { redirectTo: \`${window.location.origin}/callback\` } })`; Supabase then redirects the browser to Google. Errors fall into `toast.error(error.message || "Couldn't start Google sign-in.")` and `setLoading(false)`; the happy path doesn't reset loading because navigation is in flight. `role="separator"` + `aria-label="or continue with"` on the divider for a11y.
- `src/app/(auth)/callback/route.ts` — Next Route Handler (`GET`). Route groups don't affect URLs, so this lands at `/callback`. Flow: parse `code`, `next`, and `error` from the query; if `error` present → `302 /login?error=oauth`; if no `code` → `302 /login?error=missing_code`; otherwise create the server Supabase client and `supabase.auth.exchangeCodeForSession(code)`. If the exchange errors → `302 /login?error=auth_failed`. **Stable short error codes, not raw Supabase messages** — keeps the error URL assertable in tests and avoids leaking server-side detail. Then honor a same-origin `next` param (must start with `/` — guards against open redirect), else fetch the user via `getUser()` and look up `profiles.onboarding_completed` to pick `/dashboard` vs `/onboarding` as the destination. Uses the pattern `return NextResponse.redirect(\`${origin}${path}\`)` throughout.
- `src/types/database.ts` — added empty `Views`, `Functions`, `Enums`, `CompositeTypes` records and `Relationships: []` to every table. Supabase-js 2's `from(...).select(...)` inference walks through these namespaces — when any are missing the nested indexed access resolves to `never`, which is what caused the first build failure (`Property 'onboarding_completed' does not exist on type 'never'`).
- Even with the `Database` shape fully filled in, the projection-parser for `.select("onboarding_completed")` still inferred `never` on supabase-js's current type chain (the parser macro doesn't seem to narrow under `maybeSingle()` the way it does under `single()` in this version), so `callback/route.ts` casts the result: `(await ... .maybeSingle()) as { data: { onboarding_completed: boolean } | null }`. Kept the narrow cast rather than `.select("*")` + `Profile` so the SQL stays minimal.
- Middleware implications for `/callback`: not in `PROTECTED_PREFIXES`, not in `AUTH_PAGES`, not in `BYPASS_PREFIXES` → `updateSession` runs (setting the session cookie from the exchanged code before the route handler redirects) but no auth-gate redirect fires. This ordering matters: if `/callback` were bypassed, the cookie set by `exchangeCodeForSession` wouldn't be propagated on the redirect to `/dashboard`|`/onboarding`, and the next request would still look unauthenticated.
- `OAuthButtons` calls `createClient()` only inside the `handleGoogle` click handler (not at render), so the existing `vi.mock("@/lib/supabase/client")` in both `login.test.tsx` and `signup.test.tsx` — which doesn't expose `signInWithOAuth` — stays valid for render-only assertions. Render tests just assert the divider text + button are present; no click handler is exercised in unit tests.
- `src/__tests__/unit/login.test.tsx` — added one test (`"renders the Google OAuth button"`) asserting `getByText(/or continue with/i)` + `getByRole("button", { name: /continue with google/i })` are present. Login test count: 4 → 5.
- `src/__tests__/unit/signup.test.tsx` — same addition. Signup test count: 6 → 7.
- `src/__tests__/unit/callback.test.ts` — 4 tests, `// @vitest-environment node` pragma (Route Handler uses `NextRequest`/`NextResponse`). Hoisted mocks: `exchangeCodeMock`, `getUserMock`, `maybeSingleMock`. `@/lib/supabase/server` mock exposes `auth.exchangeCodeForSession`, `auth.getUser`, and a `from().select().eq().maybeSingle()` chain where each intermediate link is a plain `() => ...` returning the next link — only `maybeSingle` is the `vi.fn`. Redirect status assertions are tolerant (`[301, 302, 303, 307, 308]`) in case Next's default shifts. Tests:
  1. Valid code + `onboarding_completed: true` → redirect to `/dashboard`. Also verifies `exchangeCodeMock` was called with the literal code string (`"valid_code_123"`).
  2. Valid code + `onboarding_completed: false` → redirect to `/onboarding` — locks in the new-user branch.
  3. Invalid code (exchange error) → `/login?error=auth_failed` via `url.searchParams.get("error")` regex-matched to `/auth/`, AND asserts `getUserMock`/`maybeSingleMock` were **never called** — proves the exchange-error short-circuit fires before any profile work.
  4. Missing `?code` → `/login?error=missing_code` (exact), and `exchangeCodeMock` never invoked.
- Verified: `npm run build` → 4 static auth routes + `ƒ /callback` (dynamic) + `ƒ Proxy (Middleware)`. Full `npm test` → **25 passed across 6 test files** in ~10s (19 pre-Step-9 + 1 OAuth render test on login + 1 on signup + 4 new callback tests).

### Step 10 — Onboarding wizard & tests
- `src/app/(auth)/onboarding/page.tsx` — `"use client"`. 4-step wizard (Role → Industry → Review Sources → Goal). `TOTAL_STEPS = 4`, `STEP_META` keyed by step with `{title, subtitle}`. Step data arrays (`ROLES`, `INDUSTRIES`, `SOURCES`, `GOALS`) are `Option[] = { id, label, icon: IconName }`. Steps 1, 2, 4 render a 2-column `grid` of `SelectCard` (rounded card with icon + label, teal border + `color-mix(in srgb, var(--teal) 10%, transparent)` bg when selected — used the design-system `Card` component with `padding={16}`). Step 3 renders pill-shaped `SourceChip` buttons (flex-wrap, rounded 999, `aria-pressed`) — sources are multi-select, other steps are single-select. Top row shows `Step N of 4` + teal `Skip →` text button. Progress bar has `role="progressbar"`, `aria-valuemin=1`, `aria-valuemax=TOTAL_STEPS`, `aria-valuenow={step}`, and a `data-testid="progress-fill"` inner div (width = `${(step/4)*100}%`). Footer: conditional `<Back>` (outline, only `step > 1`) + coral Continue / `"Get Started →"` (last step) with `loading={saving && step === TOTAL_STEPS}`.
- `persist(payload)` posts to Supabase via `.from("profiles").update({ onboarding_data, onboarding_completed: true }).eq("id", userId)`. Same `as unknown as { update: (v) => { eq: (col, val) => Promise<{error}> } }` cast used elsewhere — supabase-js chain types infer to `never` for narrow projections in this version. Errors → `toast.error("Couldn't save. Please try again.")`. Complete flow writes `{role, industry, sources, goal}`; Skip writes `{skipped: true}`. Both route to `/dashboard` on success.
- **Race fix**: `userId` is fetched via `supabase.auth.getUser()` in `useEffect`. Added `userLoaded` boolean flag that flips true once `getUser()` resolves, and `disabled={saving || !userLoaded}` on Skip + Continue. Without this, a fast click could hit `persist()` before `userId` was set, falling through to the `if (!userId) router.push('/login')` branch — which surfaced as a flaky `skip → /dashboard` E2E test landing on `/login` instead.
- `src/middleware.ts` — `/onboarding` deliberately **NOT** in `PROTECTED_PREFIXES`. Earlier attempt added it, which broke the signup E2E: `router.push('/onboarding')` after signup triggered a middleware redirect to `/login?next=%2Fonboarding` because the client-side cookie hadn't propagated to the server yet. Page-level guard via `persist()` + `userId` covers the unauth case at action time.
- `src/__tests__/unit/onboarding.test.tsx` — 10 tests, all passing. Hoisted mocks: `pushMock`, `getUserMock`, `updateMock`, `eqMock`, `toastErrorMock`. `@/lib/supabase/client` mock exposes `auth.getUser` + a `.from().update().eq()` chain where `update` captures the payload via `updateMock` and returns `{eq: (col, val) => eqMock(col, val)}` so both call-arg assertions are available. `renderOnboarding()` helper resolves `getUserMock` with a valid user and waits on `expect(getUserMock).toHaveBeenCalled()` — this is the trick that makes tests agnostic to the `userLoaded` gate. Local helpers: `getCard(label)` finds the `role="button"` ancestor of the label span (cards are interactive `Card` components); `currentStep()` reads `aria-valuenow`; `advanceToStep(n)` picks the right selection + clicks Continue until reaching step `n`. Tests cover: step 1 renders 6 role options; selecting highlights (border turns `var(--teal)`); Continue disabled with nothing selected; enabled after selection; Back hidden on step 1; Back visible on steps 2–4; step 3 multi-select (google + yelp both `aria-pressed=true`); progress bar percentages 25/50/75/100; Skip calls update with `{skipped: true}` and pushes `/dashboard`; step 4 shows "Get Started →" and no "Continue →".
- `e2e/onboarding.spec.ts` — 5 tests, all passing. Three non-trivial helpers at the top of the file:
  - `readSupabaseUrlFromEnvFile()` synchronously reads `.env.local` and regex-matches `NEXT_PUBLIC_SUPABASE_URL\s*=\s*(.+)` (tolerates quoting + leading spaces). This is critical: `.env.local` OVERRIDES the `playwright.config.ts` `webServer.env` fallback in the dev server, so the browser client uses the real project ref. `AUTH_COOKIE_NAME` is derived as `sb-${new URL(url).hostname.split('.')[0]}-auth-token` so `seedAuthSession` sets the cookie under the exact name the GoTrue storage reader expects.
  - `seedAuthSession(page)` adds a `base64-${base64url(JSON.stringify(session))}` cookie on `localhost`, with session shape `{access_token, refresh_token, expires_at, expires_in, token_type, user: FAKE_USER}`. GoTrueClient's `_isValidSession` requires all three of `access_token`/`refresh_token`/`expires_at`; omit any and `__loadSession` rejects the seeded row.
  - `CORS_HEADERS` + OPTIONS branch in `makeProfilesPatchHandler` and `mockGetUser`. Cross-origin from `localhost:3000` → `qxnsnwnprppdzkamfbuy.supabase.co` forces a preflight; if the handler only matches the real verb and `route.fallback()`s the OPTIONS, it escapes to the real server and hangs the downstream PATCH/GET. Handler also intercepts non-PATCH verbs with a 200 + CORS instead of `fallback()` to avoid the same hang on any other profile reads.
- Tests: (1) signup → lands on `/onboarding` (regex accepts `/onboarding` or `/login?next=%2Fonboarding` — middleware/client-push race is ok either way); (2) complete all 4 steps → `/dashboard`; (3) Skip → `/dashboard`; (4) answers saved to `profiles.onboarding_data` — asserts `captured.payload.onboarding_data` has `{role: "owner", industry: "ecommerce", sources: ["google"], goal: "sentiment"}` and `onboarding_completed: true`; (5) back navigation works through all steps — asserts Back disappears on step 1 and reappears on 2/3/4, uses `.getByText(/E-commerce/i).first()` because the previous-step card text can still be in the DOM momentarily.
- Full suite: `npm test` → 39 passed across 8 files. `npx playwright test --workers=2` → 19 passed (5 onboarding + 5 signup + 5 login + 3 forgot-password + 1 sanity). `npm run build` → 7 static routes + `ƒ /callback` + `ƒ Proxy (Middleware)`.

### Step 11 — Demo data & auto-seeded sample project
- `src/lib/demo-data.ts` — 6 industry demo sets keyed by the onboarding `INDUSTRIES` ids (`ecommerce`, `saas`, `restaurant`, `healthcare`, `professional`, `agency`). Each `DemoSet` has `{ industryLabel, dbIndustry, reviews: DemoReview[], analysis: DemoAnalysis }`. 9 reviews per industry with sentiment / sentiment_score / themes pre-filled (spread: ~4 positive, ~3 negative, ~2 mixed → analysis sentiment approximately `{positive: 44, neutral: 0, negative: 33, mixed: 23}` across every set). Each `DemoAnalysis` carries a realistic summary, overall_score 68–75, 3–4 complaints with severities + examples, 2–3 praises, 1–3 feature_requests, 3 action_items with priorities, and a rating_distribution object keyed `"1"`–`"5"`. `dbIndustry` maps the onboarding id to the schema enum — `professional` → `"Other"` because the `industry` CHECK constraint doesn't include a "Services" value. `GENERIC_DEMO` = SaaS content + industry `"Other"` (fallback when the user skipped onboarding). `getDemoSet(industryId)` returns the matching set or the generic fallback.
- `src/lib/onboarding/create-demo-project.ts` — `createDemoProject({ userId, industryId?, skipped? })` returns `{ projectId, error }`. Three Supabase ops using the browser client (authenticated user, RLS picks up session): (1) `projects.insert({user_id, name, industry: dbIndustry, review_source: "Mixed", is_demo: true}).select("id").single()` where `name` is `"Sample: [industryLabel] Reviews"` or `"Sample Reviews"` when `skipped`; (2) batch `reviews.insert(demoSet.reviews.map(r => ({...r, project_id})))`; (3) `analyses.insert({...a, project_id, review_count: demoSet.reviews.length})`. All three use the same `as unknown as { insert: (v) => ... }` cast pattern used elsewhere — supabase-js chain types infer to `never` for narrow projections.
- Optional second arg `client?: SupabaseClient<Database>` — lets callers inject a mock client. Default path calls `createClient()` from `@/lib/supabase/client`.
- `src/app/(auth)/onboarding/page.tsx` — imports `createDemoProject`. Added `seedDemo(industryId, skipped)` helper that calls `createDemoProject` and logs (but doesn't surface) any error — seeding is best-effort, never blocks the `/dashboard` redirect. Wired into `handleComplete` (passes `data.industry`, `skipped=false`) and `handleSkip` (passes `null`, `skipped=true`). Both still `router.push("/dashboard")` afterward.
- Schema: `is_demo BOOLEAN DEFAULT false` was already in `supabase/migrations/001_initial_schema.sql:34` from Step 2 — no new migration.
- `src/__tests__/unit/onboarding.test.tsx` — mock update. Added `insertMock` to the hoisted vars and extended the `@/lib/supabase/client` mock: `from(table: string)` now also exposes `insert(v)` returning a chain that is both (a) awaitable — `then: (resolve) => resolve({ error: null })` — for the `reviews` and `analyses` direct-await path, and (b) has `select("id").single()` resolving `{data: {id: "demo_proj_1"}, error: null}` for the `projects` path. Without this the `skip → /dashboard` test crashed with `supabase.from(...).insert is not a function` once `createDemoProject` entered the skip flow. Test count unchanged (10) — demo seeding is invoked but its call args aren't asserted, since the coverage lives in the production code path only.
- Full suite after Step 11: `npm test` → 39 passed across 8 files. `npm run build` → 7 static routes + `ƒ /callback` + `ƒ Proxy (Middleware)`, clean TS. E2E not re-run (Playwright onboarding specs only assert `profiles.onboarding_data` PATCH and final URL, not the demo-seed inserts, so Step 11 doesn't change their behavior).

### Step 12 — Onboarding checklist component + `onboarding_completed` semantics fix
- `src/components/dashboard/onboarding-checklist.tsx` — `OnboardingChecklistCard({ checklist, userId, onDismiss? })`. Teal-bordered `Card` (1.5px accent border) with 5 items in fixed order: `account` / `survey` / `firstProject` / `firstAnalysis` / `firstExport`. Each item renders a 20px circular bubble (teal-filled + white `ok` `<Icon>` when done, hollow otherwise), label (strikethrough + `var(--tx3)` when done, `var(--tx)` otherwise; active item is weight 600), and a teal `Start →` `<Link>` ONLY on `activeItem` — defined as `allDone ? null : ITEMS.find((i) => !checklist[i.key])`, i.e. the first incomplete in array order. `account.cta = ""` so no link renders even if it's the active item (defensive: account should never be unchecked for a signed-in user). Progress row: `N of 5 complete` + `Math.round(percent)%` in teal, above a `role="progressbar"` (`aria-valuemin={0}`, `aria-valuemax={total}`, `aria-valuenow={completedCount}`) with a `data-testid="checklist-progress-fill"` inner div. Each `<li>` has `data-testid="checklist-item-${key}"` for test targeting.
- Celebration state (`allDone === true`): heading swaps to `"You're all set!"`, Dismiss button hidden, `<Confetti />` renders inside the card. Confetti is 30 absolutely-positioned 8×8px spans with `--teal`/`--coral`/`--pos`/`--warn`/`--navy` backgrounds, each with randomized `left`/`delay`/`duration`/`rotate`/`drift` via `useMemo`. CSS `@keyframes rp-confetti-fall` translates `var(--drift, 0), 240px` + rotates `720deg`, with a `@media (prefers-reduced-motion: reduce)` override that zeros animation + opacity. A 5s `setTimeout` in `useEffect(allDone)` auto-hides the card and calls `onDismiss?.()`; cleanup clears the timer on unmount.
- **SSR hydration guard**: Confetti uses `Math.random()` for every piece config — would produce a server/client mismatch if rendered during SSR. Fixed with a `mounted` state flag (initial `false`, flipped true in a `useEffect(() => setMounted(true), [])`); Confetti only mounts `{allDone && mounted && <Confetti />}`. Server renders nothing for the confetti; first client paint matches server; then mount effect flips it on. Same pattern will apply to any other Math.random/Date.now visual in dashboard components.
- Dismiss button: `aria-label="Dismiss onboarding checklist"`, disabled while `dismissing`. Writes `profiles.onboarding_completed = true` via the same `as unknown as { update: (v) => { eq: ... } }` cast pattern, then `setHidden(true)` + `onDismiss?.()`. Component returns `null` when hidden.
- `src/lib/onboarding/update-checklist.ts` — `updateChecklistItem(userId, item, value, client?)`. Fetches current `profiles.onboarding_checklist`, spreads + patches one key, writes back. Returns `{ error: string | null, checklist: OnboardingChecklist | null }`. Early-returns `"Profile not found"` when the initial `maybeSingle()` yields no row. `client?` optional arg for test injection.
- **`onboarding_completed` semantic conflict resolved**. Before Step 12, the onboarding wizard (Step 10) set `onboarding_completed = true` on survey submit, and the callback route read it to pick `/dashboard` vs `/onboarding`. Introducing the Dismiss flag on the checklist card meant both features wanted to own the same column. Split:
  - `profiles.onboarding_completed` is now **exclusively** the dashboard-checklist dismiss flag. Only `handleDismiss` in the checklist card writes it.
  - "Survey done" now lives in `profiles.onboarding_checklist.survey`. Wizard flips it via `updateChecklistItem(userId, "survey", true)` after `profiles.update({ onboarding_data })` succeeds. Checklist errors are logged via `console.warn` but don't block the flow — the user still gets redirected to `/dashboard`.
  - Callback route (`src/app/(auth)/callback/route.ts`) now selects `onboarding_checklist` and routes on `profile?.onboarding_checklist?.survey ? "/dashboard" : "/onboarding"`. Same narrow cast pattern: `{ onboarding_checklist: { survey?: boolean } | null } | null`.
- `src/app/(auth)/onboarding/page.tsx` — `persist()` payload dropped `onboarding_completed: true`; now writes only `{ onboarding_data: payload }`. After the update resolves with `error === null`, calls `updateChecklistItem(userId, "survey", true)`. A checklist-write failure surfaces as `console.warn` and returns `true` from `persist()` anyway (the primary write succeeded; the checklist is best-effort signal).
- Test updates for the semantic split:
  - `src/__tests__/unit/callback.test.ts` — mock `maybeSingleMock` now returns `{ onboarding_checklist: { survey: true/false } }`. Test 2 renamed to `"routes new users (survey not completed) to /onboarding"`. 4 tests still pass.
  - `src/__tests__/unit/onboarding.test.tsx` — `from(table)` mock extended with a `select()` chain returning `{ data: { onboarding_checklist: { account: true, survey: false, ... } }, error: null }` via `maybeSingle()` (so `updateChecklistItem`'s fetch resolves). Skip test asserts `updateMock.mock.calls[0]?.[0]` equals `{ onboarding_data: { skipped: true } }` — no more `onboarding_completed` key on the payload. Test count still 10.
  - `e2e/onboarding.spec.ts` — "answers saved" test switched from `last` PATCH (the wizard was the last write when it set `onboarding_completed`, now it's split) to `captured.find((c) => c.payload.onboarding_data)`. Dropped the `expect(last.payload.onboarding_completed).toBe(true)` assertion. Second PATCH from `updateChecklistItem` lands on the same `profiles` handler; its payload is `{ onboarding_checklist: {...} }`, not asserted. All 5 onboarding E2E tests still pass.
- `src/__tests__/unit/onboarding-checklist.test.tsx` — 7 tests, all passing (~700ms). Hoisted mocks: `updateMock`, `eqMock`. `@/lib/supabase/client` mock: `from()` returns `{ update: v => { updateMock(v); return { eq: (col, val) => { eqMock(col, val); return Promise.resolve({ error: null }); } }; } }`. Helper `makeChecklist(overrides)` seeds all 5 keys `false` then spreads overrides. Tests: (1) 5 items via `data-testid="checklist-item-${key}"`; (2) completed items → `label.style.textDecoration === "line-through"` AND `bubble.querySelector("svg")` is non-null (the `ok` icon); (3) incomplete → bubble has no SVG + label textDecoration `"none"`; (4) progress bar at 1/5 (20%) and 3/5 (60%) via `aria-valuenow` + `checklist-progress-fill` width + `/N of 5 complete/i` text; (5) `Start →` only on `checklist-item-survey` when `{account: true}` (survey's `href` is `/onboarding`); other incomplete items (`firstProject`/`firstAnalysis`/`firstExport`) return `null` for `querySelector("a")`; (6) all-complete → `/you're all set/i` heading visible, `/get started with reviewpulse/i` absent, Dismiss button absent, `checklist-confetti` visible after `waitFor` (needed because `mounted` flips in a `useEffect`); (7) Dismiss click → `/get started with reviewpulse/i` disappears, `updateMock` called with `{ onboarding_completed: true }`, `eqMock` called with `("id", "u_1")`, `onDismiss` mock invoked.
- Full suite after Step 12: `npm test` → 46 passed across 9 files in ~13s (39 prior + 7 new checklist tests).
- Known gap flagged by `functionality-validator` audit: `/dashboard`, `/projects` still don't exist — the checklist card's Start → links and all `router.push("/dashboard")` calls from login/signup/onboarding terminate in 404. Phase 3 work; intentional at this build stage. `design-validator` flagged minor off-palette items that pre-date Step 12: amber `#F59E0B` in `signup/page.tsx:28` + `icon.tsx:240-241` (should be `var(--warn)`), and `button.tsx:48-49` inline `SIZE_STYLE.padding` overriding the per-variant padding from `.btn-coral`/`.btn-navy`/`.btn-teal` CSS classes. Neither blocks Step 12.
